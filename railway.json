{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npm run migrate && node dist/src/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}